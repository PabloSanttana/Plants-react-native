import {StyleSheet} from 'react-native'

import {colors} from '../../styles'

export default StyleSheet.create({
    text:{
        marginLeft:15,
       fontSize:14,
       color: colors.gray,
       marginBottom: 17,

    },
    textMoney:{
       textAlign:'right',
       marginTop:13,
       marginRight:12,
    }
})